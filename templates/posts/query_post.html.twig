{% extends 'base.html.twig' %}

{% block title %}Results{% endblock %}

{% block body %}

    {% for post in posts %}
       <div class="text-muted pt-3">
         <div class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
           <h4><a href="{{ path('blog_show', {id: post.id}) }}">{{ post.title }}</a></h4></p>
              <p>{{ post.body | slice(0, 255) ~ '...'  }}
         </p>
       </div>
    {% endfor %}
   <a href="{{ path('blog_posts') }}"><button class="btn btn-primary mt-3">Back</button></a>
{% endblock %}